# Core dependencies for RAG QA System
langchain==0.1.0
langchain-community==0.0.10
langchain-openai==0.0.2

# Vector store
chromadb==0.4.22

# Embeddings
sentence-transformers==2.2.2

# Utilities
python-dotenv==1.0.0
tiktoken==0.5.2

# Data processing
pandas==2.1.4
numpy==1.26.3

# Evaluation (optional - for advanced metrics)
ragas==0.1.0

# For JSON parsing and handling
# (built-in, no installation needed)